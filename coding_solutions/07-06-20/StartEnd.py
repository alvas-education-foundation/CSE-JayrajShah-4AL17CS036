# CODE BY JAYRAJ

lst = list(map(lambda i: i*i, list(x for x in range(1, 31))))
print('The first 5 elements of the squared list are:', lst[:5])
print('The last 5 elements of the squared list are:', lst[-5:])
